To Run:
1. Open cmd in computer
2  run "npm install -g live-server"
3. use cd to bring the expensify-app folder
4. run "npm install" - inorder to download all the node_modules
5. run "npm start"
6. open in Chrome "localhost:3000"
